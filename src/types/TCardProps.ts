export type TFeaturedRoomProps = {
  image: string;
  name: string;
  capacity: number;
  pricePerSlot: number;
};

export type TAdsProps = {
  icon: React.ReactNode;
  title: string;
};

export type TWhyUsCardProps = {
  icon: React.ReactNode;
  title: string;
  description: string;
};
