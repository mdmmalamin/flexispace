import { TFilterProps } from "../../../types/TMeetingRoomsProps";

const FilterContainer = ({
  price,
  setPrice,
  capacity,
  setCapacity,
}: TFilterProps) => {
  // console.log({ price, setPrice, capacity, setCapacity });
  return <div>FilterContainer</div>;
};

export default FilterContainer;
