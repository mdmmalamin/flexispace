import { TSortProps } from "../../../types/TMeetingRoomsProps";


const SortContainer = ({price, setPrice}: TSortProps) => {
  console.log({price, setPrice})
  return (
    <div>
      SortContainer
    </div>
  );
};

export default SortContainer;