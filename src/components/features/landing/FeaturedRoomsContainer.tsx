import Headline from "../../ui/Headline";

const FeaturedRoomsContainer = () => {
  return (
    <div>
      <Headline>
        Room Cards: Image, Room Name, Capacity, Price Per Slot, "See Details"
        Button. See More Button: Redirects to the Meeting Rooms page.
      </Headline>
    </div>
  );
};

export default FeaturedRoomsContainer;
